name: Deploy Payment Service

on:
  push:
    branches: [main]
    paths:
      - 'backend/Services/Payment/**'
      - 'backend/Shared/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Publish
        run: dotnet publish backend/Services/Payment/HotelOS.Payment.API -c Release -o publish/payment

      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Deploy to Azure
        uses: azure/webapps-deploy@v3
        with:
          app-name: hotelos-payment
          package: publish/payment
      - name: Publish
        run: dotnet publish backend/Services/Payment/HotelOS.Payment.API -c Release -o publish/payment --no-restore
  
      - name: Clean package
        run: |
          cd publish/payment
          rm -rf *.pdb
          find . -name "*.dbg" -delete
