name: Deploy Housekeeping Service

on:
  push:
    branches: [main]
    paths:
      - 'backend/Services/Housekeeping/**'
      - 'backend/Shared/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Publish
        run: dotnet publish backend/Services/Housekeeping/HotelOS.Housekeeping.API -c Release -o publish/housekeeping

      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Deploy to Azure
        uses: azure/webapps-deploy@v3
        with:
          app-name: hotelos-housekeeping
          package: publish/housekeeping
